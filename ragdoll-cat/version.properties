# App Version Code
# Format: MAJOR(3).MINOR(2).HOTFIX(1).INTERNAL(2) - e.g. 100000 = 1.0.0.0
# Demo build: kept above the server OTA "latest" (103100) on purpose, so AppUpdateManager's
# forced-update gate is skipped and this locally-built ClassSwift actually runs on device.
versionCode=104000
